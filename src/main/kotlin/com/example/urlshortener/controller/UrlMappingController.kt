package com.example.urlshortener.controller

import org.springframework.web.bind.annotation.RestController

@RestController
class UrlMappingController {

//    POST -> client sends the long URL, we send it to the backend and create a longurl/shorturl mapping, then give back the shorturl

}